---
description: 작업 저장 — worktree 정착 안내 + working/ 문서 마무리 + 잔여 작업 판정 후 working/ 기록 (Partial) 또는 tasks/ 이동 (Done) + 본 세션 claim DISPATCH 태그 정리(done/유지/release). 짝 슬래시 = `/작업로드`·`/작업시작` (2026-05-14 신설, 분배 정리 2026-06-15)
allowed-tools: Bash, Edit, Read, Glob, Grep, PowerShell
argument-hint: "[작업명]  # 생략 시 working/ 전체 저장"
---

세션 종료 직전 호출하는 통합 저장 슬래시. `/working-done` (단순 이동) 과 `/자동진행` (실행 모드 진입) 의 중간 — **세션 마감 저장** 역할. 짝 슬래시 = **`/작업로드`** (다음 세션 시작 시 잔존 작업 불러오기). **+ 본 세션이 `/작업시작` 으로 claim 한 DISPATCH 태그 작업도 working 문서 Done/Partial 판정에 연동해 함께 정리** (완료 → `dispatch_done` / 이어감 → claimed 유지 / 포기 → `dispatch_release`).

## 동작 3단계

| 단계 | 동작 | 결과 |
|------|------|------|
| ① worktree 정착 안내 | `git worktree list` 스캔 → `~/.claude/worktrees/` 존재 시 정착 명령 안내 | 사용자 직접 실행 (`! ` prefix) |
| ② working/ 문서 마무리 | working/ 스캔 → 각 파일 잔여 작업 판정 | Status 결정 (Done / Partial) |
| ③ 잔여 작업 기록 / 이동 | 잔여 0건 = `Status: Done` 부착 → working-lifecycle 자동 이동 / 잔여 ≥ 1 건 = `Status: Partial` + `## 잔여 작업` 섹션 추가 → working/ 유지 (다음 세션 `/작업로드` 진입점) | 분기 처리 |
| ④ 분배 태그 정리 | 본 세션 sid 가 claim 한 DISPATCH 태그 조회 → 완료판정 연동 (완료 `dispatch_done` / 이어감 claimed 유지 / 포기 `dispatch_release`) | 분배 풀 정합 |

## 호출 방식

| 인자 | 동작 |
|------|------|
| 인자 없음 (`/작업저장`) | worktree 전체 스캔 + working/ 전체 일괄 저장 |
| `{작업명}` (`/작업저장 auth-refactor`) | 파일명 매칭 → 특정 작업만 저장 |

## ① worktree 정착 안내

진입 즉시 다음 확인:

```bash
git worktree list 2>/dev/null
```

- `~/.claude/worktrees/{session_id}-{slug}` 패턴 worktree 존재 시 → 사용자에게 정착 6단계 안내 (자동진행 §"정착 절차" 와 동일):

```bash
# 사용자 직접 실행 (! prefix)
! cd {원본 repo}
! git checkout {source-branch}
! git checkout -b feature/{source-branch}_{slug}
! git merge --ff-only wip/{session_id}-{slug}
! git worktree remove ~/.claude/worktrees/{session_id}-{slug}
! git branch -D wip/{session_id}-{slug}
```

**제약:**
- Claude 본체가 5~6 단계 직접 실행 시 `branch-enforce.sh` 차단. 안내만.
- source = `main` / `master` 시 정착 절대 금지 — PR 생성 안내로 대체.
- 정착 후 `git push` 도 사용자 직접.
- worktree 없으면 본 단계 SKIP.

## ② working/ 문서 마무리

`~/.claude/docs/working/YYYYMMDD/` 스캔. 인자 매칭 시 해당 파일만.

각 파일에 대해:

1. **Self-Critique 섹션 확인** — 미존재 시 Claude 본체가 직접 작성 (template 보강)
2. **잔여 작업 판정** — 다음 신호 종합:
   - 본문 미체크 박스 (`- [ ]`) 개수
   - 사용자 명시 잔여 (인자 또는 직전 응답 후속 권고 잔여)
   - `## TODO` / `## 잔여` / `## Follow-up` 섹션 존재 + 비어있지 않음
   - Self-Critique 항목 중 FAIL/WARN 미해결

## ③ 잔여 작업 기록 / 이동

### 분기 A — 잔여 0건

```markdown
Status: Done
```

(시작 라인) 부착 + `## Self-Critique` 보강 → `working-lifecycle.sh` PostToolUse hook 자동 발동 → `~/.claude/docs/{product}/tasks/YYYYMMDD/{작업명}/{yyyy-mm-dd}-{작업명}-unified.md` 이동.

### 분기 B — 잔여 ≥ 1건 (`/작업로드` 진입점)

```markdown
Status: Partial
```

(시작 라인) + 본문 끝에 `## 잔여 작업` 섹션 자동 부착:

```markdown
## 잔여 작업

- [ ] {잔여 항목 1} — 사유 / 다음 단계 1줄
- [ ] {잔여 항목 2} — 사유 / 다음 단계 1줄
...

**다음 세션 진입점:**
- `/작업로드` — 본 잔존 작업 자동 식별 + 재진입 안내
- `/작업로드 {작업명}` — 본 작업만 불러오기
- `/자동진행 {요약}` — 잔여 일괄 처리 (`/작업로드` 후 결정)
```

→ working/ 원본 유지 (이동 SKIP). 다음 세션에서 `/작업로드` 호출 → 잔여 항목 본문 표시 → 처리 후 다시 `/작업저장` 시 잔여 0건이면 자동 이동.

### REGISTRY 갱신 (2026-05-15 신설)

본 슬래시 ② working/ 문서 마무리 직후 `~/.claude/docs/working/REGISTRY.md` 갱신:

| 분기 | REGISTRY 처리 | 갱신 주체 |
|------|--------------|----------|
| 분기 A (Done) | entry 자동 제거 + state/sessions/{slug}/ lock 제거 | `working-lifecycle.sh` 가 mv 직후 자동 호출 (별도 액션 불필요) |
| 분기 B (Partial) | entry status=paused 명시 갱신 | `source ~/.claude/hooks/lib/registry-utils.sh && registry_update {slug} {sid} paused` 명시 호출 |

**충돌 정책:** 본 세션 외 다른 세션이 동일 slug 점유 (active) 중일 시 status=paused 갱신은 본 세션 sid entry 만 영향 — 다른 세션 active entry 는 보존.

## ④ 분배 태그 정리 (DISPATCH, 2026-06-15 신설)

② working/ 문서 Done/Partial 판정과 **대칭**으로, 본 세션이 `/작업시작` 으로 claim 한 DISPATCH 태그를 세션 마감 시 정리한다. **본 세션 claim 분만 대상** — 분배만 하고 미claim된 `available` 태그는 비대상 (풀 유지, 타 세션용). 타 세션이 claim 한 태그는 건드리지 않는다.

### 본 세션 claim 태그 식별 (sid 기반)

```bash
source ~/.claude/hooks/lib/dispatch-utils.sh
# 본 세션 sid — REGISTRY 갱신과 동일 체계 (환경 미노출 시 Claude 본체 기입)
SID8="${CLAUDE_SESSION_ID:0:8}"
[ -z "$SID8" ] && SID8="<현재 세션 sid 8자 — Claude 본체 기입>"

# 본 세션 claim 태그 = claimed_by == SID8 (claimed_by 는 표 6번째 컬럼)
dispatch_list claimed | awk -F"$DISPATCH_FS" -v s="$SID8" '
  $2!="tag" && $2!="" && $6==s { printf "%s\t%s\n", $2, $9 }
'
# (보강) lock 파일 교차 확인 — state/dispatch/{tag}/{SID8}.lock 보유분
for d in ~/.claude/state/dispatch/*/; do
  [ -f "$d$SID8.lock" ] && basename "$d"
done
```

### 태그별 완료 판정 → 동작

각 본 세션 claim 태그에 대해 — 연결된 working 통합 문서의 Done/Partial 또는 분배 문서 DoD 체크박스로 완료 판정:

| 판정 | 동작 | 명령 |
|------|------|------|
| **완료** (working Done / DoD 전부 `[x]`) | status=done + lock 정리 | `dispatch_done "{tag}" "$SID8"` |
| **이어감** (working Partial / 잔여 ≥1) | claimed 유지 (다음 세션 `/작업시작 #tag` 으로 재개) | (mutation 없음 — 유지) |
| **명시 포기** (이 세션 더 안 함, 타 세션 이양) | available 복귀 | `dispatch_release "{tag}" "$SID8"` |

> **세션 완전 종료 일괄 release (2026-06-15):** 이어갈 태그 없이 세션을 마칠 때는 본 세션 claim 전체를 한 번에 풀 수 있다 — `dispatch_release_session "$SID8"` (claim 태그 전부 `available` + dispatch lock 제거) + `registry_release_session "$SID8" paused` (REGISTRY entry + session lock). **Stop hook (`working-release.sh`) 이 세션 종료 시 동일 동작을 자동 수행**하므로, 슬래시 없이 종료해도 orphan claim/lock 은 남지 않는다 (본 ④ = 명시 정리 진입점, Stop = 안전망).

**안전장치 (필수):** **완료 판정이 명확한 태그만 자동 `dispatch_done`.** 모호하거나 외부 게이트 대기 (예: `claimed` 인데 DoD 미충족·SES/DB 등 외부 차단) 는 자동 done 금지 — 목록 제시 후 사용자 확인. 미완료를 done 처리하면 종결 신호가 거짓이 되어 추적성이 손상된다.

### 보고

```
[Claude] ④ 분배 태그 정리 (본 세션 claim N건):
  - #auth-jwt    → done (working Done 연동)
  - #auth-routes → claimed 유지 (working Partial, 다음 세션 이어감)
  - #auth-test   → [확인 필요] DoD 미충족 — 자동 done 보류
```

## 터미널 제목 원복 (세션 마감)

세션 마감이므로 진행 중 설정됐던 `#{작업명}` 터미널 제목을 현재 폴더명으로 리셋한다 — PowerShell 도구로 `$Host.UI.RawUI.WindowTitle = (Split-Path -Leaf $PWD)` 실행. 방식·전제 = `commands/작업시작.md` §"터미널 제목 설정 (SSOT)". 비-Windows·실패 시 무시.

## §3 Checkpoint 우선 적용

- worktree 정착 5~6 단계 = 비가역 (`git merge` / `git worktree remove` / `git branch -D`) → 사용자 명시 승인 후 직접 실행.
- working/ 문서 Status 변경 + 잔여 작업 섹션 추가 = Edit 도구 사용, `~/.claude/docs/working/` 경로는 Gate-0 면제.
- master/main 머지 절대 금지 룰 우선 — source 가 main/master 시 정착 안내 자체 생략, PR 안내 대체.
- **분배 태그 정리 (`dispatch_done`/`dispatch_release`) = DISPATCH.md + lock mutation.** 회복 가능(`dispatch_add` 재등록)이나 타 세션 가시 상태 변경 → **완료 판정 명확분만 자동, 모호분은 사용자 확인.** done/release 모두 **claim 본인 sid 만** 정상 동작 — 타 세션 claim 태그는 건드리지 않는다.

## SSOT

| SSOT | 역할 |
|------|------|
| `~/.claude/CLAUDE.md` §File Paths "working/ 단일 통합 문서" + §4.3 "branch·worktree·push·머지 통합 정책" | 정책 SSOT |
| `~/.claude/hooks/working-lifecycle.sh` | Status: Done 자동 이동 본체 |
| **`~/.claude/hooks/lib/dispatch-utils.sh`** | **본 세션 claim DISPATCH 태그 정리 (`dispatch_done`/`dispatch_release`/`dispatch_list claimed`) — ④ 단계** |
| **`~/.claude/commands/작업분배.md`** | **분배 작업 생성자 (DISPATCH 등록) — 본 슬래시 ④가 정리하는 태그의 작성 측** |
| **`~/.claude/commands/작업시작.md`** | **분배 태그 claim 진입점 — 본 슬래시 ④가 정리하는 claim 의 생성자 (claim↔정리 짝)** |
| `~/.claude/hooks/branch-enforce.sh` | worktree 분기 강제 + 정착 단계 보호 |
| `~/.claude/skills/task-docs/references/unified-template.md` | 양식 SSOT (잔여 작업 섹션 포함) |
| `~/.claude/commands/working-done.md` | 본 슬래시의 ③-A 분기 (단순 이동) 단독 진입점 |
| `~/.claude/commands/자동진행.md` | 본 슬래시의 ① worktree 정착 절차 SSOT |
| **`~/.claude/commands/작업로드.md`** | **짝 슬래시 — Status: Partial 잔존 작업 불러오기 (다음 세션 진입점)** |
| `~/.claude/commands/작업저장.md` (본 파일) | 세션 마감 통합 저장 — worktree + 문서 + 잔여 동시 처리 |
| **REGISTRY 갱신 책임 (audit M13 명문 2026-05-20)** | **쓰기 — entry add / Status: Partial 보관 / Status: Done 시 working-lifecycle hook 가 entry 제거** |

## 호출 예

```
/작업저장                            ← 전체 (worktree 스캔 + working/ 전체)
/작업저장 auth-refactor              ← 특정 작업만
```

세션 마감 흐름 예시:

```
[사용자] /작업저장
   ↓
[Claude] ① worktree 스캔 → 1건 발견: ~/.claude/worktrees/631cd229-auth-refactor
        ② working/ 스캔 → 2건 발견:
           - 2026-05-14-hongcafe_global_backend-auth-refactor.md → 잔여 0건
           - 2026-05-14-claude-harness-hook-cleanup.md → 잔여 3건 (미체크 박스)
        ③ 처리:
           - auth-refactor → Status: Done 부착 → 자동 이동 (working-lifecycle 발동)
           - hook-cleanup → Status: Partial + ## 잔여 작업 섹션 추가 → working/ 유지
        ④ 정착 안내:
           "다음 명령을 직접 실행해주세요:
              ! cd /path/to/repo && git checkout production
              ! git checkout -b feature/production_auth-refactor
              ! git merge --ff-only wip/631cd229-auth-refactor
              ! git worktree remove ~/.claude/worktrees/631cd229-auth-refactor
              ! git branch -D wip/631cd229-auth-refactor"
        ④ 분배 태그 정리:
           - 본 세션 claim 태그 조회 (sid 기반) → 해당 0건이면 SKIP
        ⑤ 보고:
           - 이동 1건 / 잔여 보관 1건 / worktree 정착 대기 1건
           - 분배 태그 정리: 본 세션 claim 0건 (해당 없음)
           - 다음 세션 진입: /작업로드
```

## 차별점 (다른 슬래시와)

| 슬래시 | 시점 | 범위 |
|--------|------|------|
| `/자동진행` | 작업 시작·중간 | 묶음 승인 모드 진입 (실행) |
| `/working-done` | 작업 완료 직후 | working/ → tasks/ 단순 이동 (분기 A 만) |
| **`/작업저장`** | **세션 마감 직전** | **worktree + 문서 + 잔여 + 본 세션 claim 분배 태그 정리 (분기 A + B + worktree + DISPATCH ④)** |
| **`/작업로드`** | **다음 세션 시작 직후** | **Status: Partial 잔존 작업 식별 + 재진입 안내** |
