---
description: 일일·주간·월간 업무 리포트 생성. ~/.claude/docs/{product}/tasks/ 작업 이력 기반 그룹화·진행률 판정
allowed-tools: Skill, Bash, Read, Glob, Grep
argument-hint: "[daily|weekly|monthly] [날짜 또는 주차]"
---

본 슬래시 = `skills/report` SSOT 의 thin wrapper.

오늘 날짜 기준 업무 리포트를 생성한다.

## 데이터 소스

모든 작업 산출물은 **글로벌 루트** `~/.claude/docs/{product}/tasks/` 하위에 통합 관리된다.

- 탐색 루트: `~/.claude/docs/*/tasks/`
- `{product}` 식별: 디렉토리명이 곧 product 이름 (`basename` 기반)

## product → [태그] 매핑

| product 디렉토리 | 태그 |
|---|---|
| `hongcafe_global_backend` | `[백엔드]` |
| `hongcafe_global_frontend` | `[프론트]` |
| `infra` | `[인프라]` |
| `claude-harness` | `[하니스]` |
| `hongcafe3` / `hongcafe-japan` 등 레거시 | `[레거시]` |
| 그 외 | `[{product}]` (원본 이름 그대로) |

> 매핑에 없는 신규 product 가 나오면 원본 이름 그대로 표기하고 사용자에게 매핑 등록 여부를 확인한다.

## 수행 절차

1. `~/.claude/docs/*/tasks/` 글로브로 **모든 product 디렉토리** 일괄 스캔.
2. 각 product 의 `history.md` 에서 대상 기간(오늘/주간/월간) 항목을 확인.
3. 작업 폴더 `~/.claude/docs/{product}/tasks/YYYYMMDD/` 하위 서브디렉토리를 순회. 파일명은 `{yyyy-mm-dd}-{작업명}-{type}.md` prefix 규칙(task-docs SKILL §산출물 네이밍 규칙)을 따른다:
   - `*-analyze.md` 만 존재 → **분석 중 (30%)**
   - `*-analyze.md` + `*-plan.md` → **계획 완료 (50%)**
   - `*-analyze.md` + `*-plan.md` + `*-result.md` → **완료**
   - `*-analyze.md` 또는 `*-result.md` 단독 (산출물 유연성, CLAUDE.md §4) → 해당 문서의 체크리스트 체크 비율로 진행률 추정 (체크 0% = 30%, 50~99% = 50%, 100% = 완료)
4. `YYYYMMDD/summary.md` 에서 완료/진행 중 구분 문구를 참고로 삼는다.
5. `YYYYMMDD/todo.md` 가 있으면 미완료 항목은 `(미착수)` 로 추가.
6. 진행률 판정이 서로 상충하면 `history.md` 기록을 우선.
7. **주제별 그룹화**: 여러 날짜·여러 task 디렉토리가 **같은 주제**이면 하나의 그룹으로 묶는다.
8. **언어 평문화 + 보고 정제**: 내부 코드네임·사내 약어·영문 기술 용어는 한국어로 풀고, **보고에 가치 없는 메타 디테일은 제거**한다 (아래 §보고 정제 룰 참조).

## 출력 형식

### 일일 리포트 (`/report`)

간단 1줄 형식 (날짜는 헤더에 한 번만):

```
📋 업무 리포트 — YYYY.MM.DD

[글로벌]-[인프라]-EC2 PHP 설정 반영 (완료)
[글로벌]-[백엔드]-API 인증 필터 추가 (50%)
[글로벌]-[프론트]-로그인 페이지 구현 (완료)
[글로벌]-[하니스]-docs 경로 글로벌 이관 (50%)
[글로벌]-[인프라]-CloudFront + HTTPS 적용 (미착수)
```

### 주간/월간 리포트 (`/report weekly`, `/report monthly`)

주제별 그룹 + 트리 sub-item. **날짜는 sub-item 맨 앞**에 위치하며, 날짜 컬럼 정렬 후 작업 요약 한 줄. 그룹 끝은 `└ ✅ 완료 — 결론` 또는 `└ 🟡 NN% — 사유`.

```
📋 업무 리포트 — YYYY.MM.DD ~ YYYY.MM.DD

[글로벌]-[태그]-주제 그룹 제목 (평문화)
    └ MM.DD       작업 요약 한 줄 (효과 중심)
    └ MM.DD       작업 요약 한 줄
    └ MM.DD~DD    같은 주제 다일자 묶음
    └ ✅ 완료 — 한 줄 결론

[글로벌]-[태그]-다음 주제 그룹
    └ MM.DD       …
    └ 🟡 50% — 결정 대기 사유 한 줄
```

세부 항목이 여러 갈래로 갈리면 sub-item 안에서 `•` 들여쓰기로 2단계까지만 허용:

```
    └ MM.DD       프론트·백엔드·인프라 3영역 풀스택 디버그
                    • 운영 차단 잠재 위험 5건 + 중요 12건 식별
                    • 신규 모듈 배포 보류 권고 (프론트·인프라 미연동)
```

## 규칙

### 공통
- 각 업무는 `[글로벌]-[태그]-업무내용` 형식. `[글로벌]` 항상 앞에 붙임. 하이픈(`-`) 구분.
- 두 번째 태그로 product 태그 (위 매핑 표) 부여.
- 여러 product 에 걸친 공통 작업은 가장 대표적인 태그 1개, 공통 성격이면 `[공통]`.
- 진행률은 task 디렉토리 산출물 기준 자동 판단.
- `todo.md` 의 미완료 항목은 `(미착수)` 로 표시.
- 오늘 작업이 없는 product 는 리포트에서 제외 (과거 진행 중 건이 남아있으면 `(진행중)` 으로 별도 표시).

### 언어 평문화 (필수)
- **내부 코드네임 그대로 쓰지 말 것**: `p2-p3-execution-plan` → `백엔드 백로그 통합 실행 계획`, `nginx-bff-loopback` → `Nginx BFF 루프백 구성`
- **사내 약어 풀어쓰기**: `P2/P3 이월 항목` → `미완료 중·저 우선순위 백로그`, `IDOR` → `타인 데이터 조회 가능성(IDOR 취약점)`
- **영문 기술 용어 한국어 병기**: `Checkpoint` → `승인 지점`, `rewrite` → `URL 재작성`, `keepalive` → `연결 유지`
- **목적·효과 중심 기술**: 파일명·함수명 그대로가 아니라 "무엇을 바꿔서 어떤 효과를 얻었는지"
- 처음 보는 사람이 읽어도 이해 가능한지 자문 후 출력

### 보고 정제 (필수 — 의사결정자 가독성 우선)
보고에 **들어가야 하는 것**:
- 무엇을 / 어떤 효과 / 운영 영향
- 운영 차단·결정 대기 같은 핵심 시그널
- 수치는 의미 있는 것만 (회수 용량 / 처리 건수 / 커버리지 % / 등급 변화)

보고에서 **빼야 하는 것** (보고서 가치 없음):
- commit 해시·SHA·branch 이름
- 파일명·함수명·라인 번호 (`BaseController:142` 같은 표기)
- hook 이름·script 이름 (효과만 적고 도구명은 생략)
- Phase 1/2/3, Round 1/2/3, Agent A/B/C/D 같은 내부 단계 번호
- mock·assertion 개수, 테스트 케이스 단위 수치 (큰 통과/실패 신호만)
- "메모리 갱신", "git commit 분리", "history.md 기록" 같은 메타 작업
- 산출물 경로·문서 ID

압축 가이드:
- 한 그룹의 sub-item 은 5~7개 이내. 같은 흐름이면 묶고 같은 날짜 다중 작업도 한 줄로 통합 (`핫픽스 2건 + 자격증명 정합`).
- 한 줄 30~80자 권장. 80자 초과 시 두 갈래(`•`)로 분기.
- 같은 주제 여러 날짜는 `MM.DD~DD` 표기.

### 주제 그룹화 (주간·월간)
- 같은 주제·같은 흐름 task 는 하나의 그룹으로 묶음.
  - `nginx-*` 다수 → `Nginx 라우팅 재설계 + prd/로컬 싱크`
  - `api-test-*` + `phpunit-*` + `b1-logout-*` → `phpunit 테스트 인프라 안정화`
- 그룹 제목은 평문화 규칙 적용. 길이는 영문/한국어 혼용 50자 이내 권장.
- sub-item (`└`) 은 실제 task 디렉토리 기반이되, 내부 이름은 평문 설명으로 교체.
- 그룹 끝 `└` 한 줄로 상태 명시: `✅ 완료 — 결론` / `🟡 NN% — 사유` / `🔴 차단 — 사유`.

### 출력 간결성
- 일일 리포트: 한 줄에 하나의 업무, 그룹화 불필요.
- 주간·월간 리포트: 주제 그룹화 + 트리 sub-item (날짜 prefix) + 상태 라인.

## 주간/월간 확장

- 주간: `~/.claude/docs/*/tasks/YYYYMMDD/` 를 최근 7일 범위로 확장 스캔.
- 월간: 최근 30일 범위. 각 날짜의 `summary.md` 타이틀만 수집해도 충분.
- 인자 예시:
  - `/report` — 오늘
  - `/report weekly` — 이번 주 월~오늘
  - `/report monthly` — 이번 달 1일~오늘
  - `/report YYYY-MM-DD부터 YYYY-MM-DD까지` 또는 `MM-DD일부터 오늘까지` — 자유 기간 (한국어 자연어 파싱 허용)

## 예시 (참고용 — 주간 형식, 채택된 표준)

```
📋 업무 리포트 — 2026.04.25 ~ 2026.04.30

[글로벌]-[백엔드]-소셜로그인(SNS) 안정화 + 인증 쿠키 통합
    └ 04.27  Google 로그인 100% 차단 운영장애 핫픽스 (일 6건/일 fatal)
    └ 04.27  SNS 가입 차단 핫픽스 2건 + OAuth 자격증명 정합 (Google/FB/Apple)
    └ 04.27  SNS v3 EP 재설계 — 책임 분리, 가입 폼 자동 채움 응답 추가
    └ 04.27  응답 쿠키 누락 회귀 근본 차단
    └ 04.28  인증 쿠키 발급 단일 출처 통합 — 4개 컨트롤러 흩어진 로직 모음
    └ 04.28  PHP 8 잔존 코드 제거 + API 디버그 로그 자동 주입
    └ ✅ 완료 — 운영 머지 / 회귀 테스트 통과

[글로벌]-[백엔드]-API 명세·계약 정합 + 풀스택 영향 검증
    └ 04.27       IEEE 명세 26파일 일괄 갱신 — 1주일 누적 코드↔명세 차이 해소
    └ 04.28~30    미사용 비번찾기 API 2건 식별 — 프론트 사용 0건 확정
    └ 04.30       로그아웃 응답 에러코드 표준화
    └ 04.30       프론트·백엔드·인프라 3영역 풀스택 디버그
                    • 운영 차단 잠재 위험 5건 + 중요 12건 식별
                    • 신규 모듈 배포 보류 권고 (프론트·인프라 미연동)
    └ 🟡 50% — 처리 우선순위 사용자 결정 대기

[글로벌]-[인프라]-EC2 운영 환경 정리 + 로컬 개발 스택
    └ 04.27  EC2 디스크 6G 회수 + 옛 릴리스 자동 회수 cron 배포
    └ 04.27  운영 ↔ 로컬 환경 동기화 (deploy 스크립트·env 디렉토리)
    └ 04.27  WSL Ubuntu 폐기 — 약 50GB 디스크 회수
    └ 04.27  통합 개발 Docker 스택 신설 (웹·앱·DB·캐시 5 서비스 자동 마운트)
    └ 04.29  인프라 IaC 정합 — 운영 컴포넌트 백업 + 인프라 문서 갱신
    └ ✅ 완료 — 운영 트래픽 영향 0

[글로벌]-[하니스]-Claude 자동화 강제·검증 인프라 구축
    └ 04.27  스킬 자동 검증 도구 v2.0 — 4영역 병렬 검증
    └ 04.27  메모리 행동 룰 18건 → 지침·hook 단일 출처로 코드화 이전
    └ 04.27  묶음 승인 fast-track — 승인 단계 1회로 단축
    └ 04.28  자동화 빈공간 14개 일괄 hook 화
    └ 04.29  hook 운영 결함 4건 fix + 정적 검증 도구 신설
    └ 04.30  자동화 강제 커버리지 64% → 85%
    └ ✅ 완료
```
